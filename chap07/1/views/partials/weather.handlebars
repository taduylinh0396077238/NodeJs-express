<div class="weatherWidget">
    {{#each partials.weatherCotext}}
        <div class="location">
            <h3>{{location.name}}</h3>
            <a href="{{location.forecastUrl}}">
                <img src="{{iconUrl}}" alt="{{weather}}">
                {{weather}}, {{temp}}
            </a>
        </div>
    {{/each}}
    <small>Sourse: <a href="https://www.weather.gov/documentation/services-webapi">
    National Weather Service</a></small>
    </a></small>
</div>